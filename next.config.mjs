/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com', 'a0.muscache.com', 'atbbixtemoxbbezyxiop.supabase.co'],
    },
};

export default nextConfig;
